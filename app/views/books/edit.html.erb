
<h1>Edingbooks</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %> errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


<%= form_with model:@book do |t| %>

<p>title</p>
<%= t.text_field:title %>
<p>body</p>
<%= t.text_area:body %>

<%= t.submit 'Update Book' %>
<% end %>
<div><a><%= link_to "Show" , book_path(@book.id) %></a> |<a href='/books'>Back</a></div>